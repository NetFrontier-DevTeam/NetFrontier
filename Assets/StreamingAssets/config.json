{
  "Database": {
    "Server": "localhost",
    "DatabaseName": "netfrontier_db",
    "UserId": "netfrontier_admin",
    "Password": "********" // if your using a localhost use whatever your db pw is.
  }
}
